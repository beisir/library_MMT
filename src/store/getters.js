export default {
    searchListParams (state) {
        return state.searchListParams;
    },
    asideCur (state) {
        return state.asideCur;
    },

    // proContarst路由组件存储选择状态
    statusCatid (state) {
        return state.statusCatid;
    },
    statusList (state) {
        return state.statusList;
    }
}
